# Часть A — предскажи вывод

Пиши ответ И пояснение. «Не знаю» — нормальный ответ.

A1: ты дал ответ в своем .md
    почему:

A2:напишется кодировка строки 'A', затем измененная строка на следующий символ в таблице, и затем напишется объем памяти, занимаемый строкой 'A'
    почему: в принтф прописаны разные типы данных, я их не совсем понимаю, но д - числа, а ц - это символы, крайний тип для меня вообще не очень понятен.

A3:непредсказуемо, вероятно ошибка ваще
    почему мы не задали никакое значение переменной, а только тип данных:

A4:мне трудно ответить "с лёту", но так на первый взгляд будто бы выйдут 12 и 5, либо вся формула сломается из-за операцией над одной и той же переменной хотя я то же самое делал у себя в первом задании, так что наверное не должно
    почему:

A5: 0 3 6 9
    почему:
сначала он сверяется, что переменная меньше 10, затем проводит действие в цикле, затем делает +3, в итоге к моменту дохождения до 12 он сверяет - уже больше 10, ниче не делаю, цикл завершен!

A6: 1 не знаю 0
    почему: в массивах всё считается от 0, поэтому 0 элемент это 1, а 5 элемента нет ваще. а насчет * - я не помню как работают эти указатели

A7: не знаю брат
    почему:

A8: больше
    почему:
при беззнаковом типе данных самое маленькое будет 0, если от малого числа отнять большее - произойдет переполнение и число пойдет с другого конца диапазона, который так и так больше нуля. либо я ошибся и unsigned просто так не существует xDD

A9: самое малое число дипазона int
    почему: вызвалось переполнение и мы пошли двигаться с другого конца диапазона (я не помню рамки диапазонов у типов данных)

A10:
я не знаю или не помню че делают указатели рядом с переменными    почему:

A11:
не-а    почему вот это для меня совсем точно было решено через иишку когда я сдавал задание по учебе

A12:
черт возьми я не понимаю че это)    почему:
вжизни не видел---

# Часть C — самоотчёт

1. Где и как учился, сколько часов: я учусь в колледже на специальности "компьютерные системы и комплексы", сишку сиплюсы сдал на 4, была дисциплина "основы алгоритмизации и программирования"
2. Что напишешь с нуля без подглядывания (цикл / функция / массив / указатель / структура / класс): может получатся примерно первые три из 6
3. ОС и компилятор, настроенная среда: я настроил себе vs code, запускаемый из-под терминала wsl - установил нужные расширения и для сразу пула в наш репо. сам живу в винде, но наша работа в вслке происходит
4. Собирал ли проект из нескольких файлов? Пользовался ли gdb / санитайзерами: никогда в жизни
5. Цель изучения C/C++: интересно, любопытно, и есть у меня некоторая слабая страсть к низкоуровневому программированию, люблю "железо" и дрался с ардуинкой
6. Реальных часов в неделю: ьлин ну я думаю что как пойдет, я всё же в основном щас по специальности учусь, вот вышел на 3 курс
