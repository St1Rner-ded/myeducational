# Справочник C / C++

> **Живой документ.** Версия **1.0** · 2026-09-14 · обновляется по мере прохождения курса.
> Лежит в корне репозитория как `SPRAVOCHNIK.md` → открывается на GitHub с любого устройства.
>
> Все числа здесь получены реальным запуском **gcc 12.2, x86-64 Linux**.
> Пометки: ✅ — гарантировано стандартом · ⚙️ — зависит от платформы.

## Как обновлять

1. Я присылаю новую версию файла (в чат открываю `cpp-course/SPRAVOCHNIK.md`).
2. Ты скачиваешь, кладёшь в корень репо поверх старого, коммитишь:
   ```bash
   git add SPRAVOCHNIK.md && git commit -m "spravochnik v1.1" && git push
   ```
3. Что нового — смотри в разделе [11. Журнал изменений](#11-журнал-изменений).

---

## Содержание

1. [Типы данных](#1-типы-данных)
2. [Ввод-вывод: printf / scanf](#2-ввод-вывод-printf--scanf)
3. [Массивы, указатели, строки](#3-массивы-указатели-строки)
4. [Переполнение и неопределённое поведение](#4-переполнение-и-неопределённое-поведение)
5. [Битовые операции](#5-битовые-операции)
6. [Структуры и память](#6-структуры-и-память)
7. [Компиляция и инструменты](#7-компиляция-и-инструменты)
8. [Отличия C от C++](#8-отличия-c-от-c)
9. [Каталог частых ошибок](#9-каталог-частых-ошибок)
10. [Словарь терминов](#10-словарь-терминов)
11. [Журнал изменений](#11-журнал-изменений)

---

## 1. Типы данных

### 1.1 Базовые целые (x86-64)

| Тип | Байт | Минимум | Максимум | Макрос (`<limits.h>`) |
|---|---|---|---|---|
| `char` | 1 | −128 | 127 | `CHAR_MIN` / `CHAR_MAX` |
| `signed char` | 1 | −128 | 127 | `SCHAR_MIN` / `SCHAR_MAX` |
| `unsigned char` | 1 | 0 | 255 | `UCHAR_MAX` |
| `short` | 2 | −32 768 | 32 767 | `SHRT_MIN` / `SHRT_MAX` |
| `unsigned short` | 2 | 0 | 65 535 | `USHRT_MAX` |
| `int` | 4 | −2 147 483 648 | 2 147 483 647 | `INT_MIN` / `INT_MAX` |
| `unsigned int` | 4 | 0 | 4 294 967 295 | `UINT_MAX` |
| `long` | ⚙️ **8 в Linux, 4 в Windows** | −9 223 372 036 854 775 808 | 9 223 372 036 854 775 807 | `LONG_MIN` / `LONG_MAX` |
| `unsigned long` | ⚙️ 8 / 4 | 0 | 18 446 744 073 709 551 615 | `ULONG_MAX` |
| `long long` | 8 | −9 223 372 036 854 775 808 | 9 223 372 036 854 775 807 | `LLONG_MIN` / `LLONG_MAX` |
| `unsigned long long` | 8 | 0 | 18 446 744 073 709 551 615 | `ULLONG_MAX` |
| `size_t` | ⚙️ 8 (64-бит) | 0 | 18 446 744 073 709 551 615 | `SIZE_MAX` |
| `_Bool` / `bool` | 1 | 0 | 1 | — |
| любой указатель | ⚙️ 8 | — | — | — |

Гарантии стандарта (✅): `sizeof(char) == 1`; порядок `char ≤ short ≤ int ≤ long ≤ long long`;
`CHAR_BIT ≥ 8`. Конкретные размеры — на совести платформы.

### 1.2 Фиксированная ширина — `<stdint.h>` (C) / `<cstdint>` (C++)

Размер одинаков **везде**. Используй их, когда важен точный размер (регистры, протоколы, файлы):

| Тип | Байт | Тип | Байт |
|---|---|---|---|
| `int8_t` | 1 | `uint8_t` | 1 |
| `int16_t` | 2 | `uint16_t` | 2 |
| `int32_t` | 4 | `uint32_t` | 4 |
| `int64_t` | 8 | `uint64_t` | 8 |

Границы: `INT32_MAX`, `UINT32_MAX`, … из того же заголовка.
Печать таких типов — только через макросы из `<inttypes.h>` (см. 2.4).

### 1.3 Вещественные

| Тип | Байт | Знаков мантиссы | Мин (норм.) | Макс |
|---|---|---|---|---|
| `float` | 4 | 6–7 | 1.175494e−38 | 3.402823e+38 |
| `double` | 8 | 15–16 | 2.225074e−308 | 1.797693e+308 |
| `long double` | ⚙️ 16 в Linux (x87, 80 бит), **8 в Windows/MSVC** | 18–19 | — | — |

- **Никогда не сравнивай float/double через `==`.** Только `fabs(a - b) < eps`.
- `0.1 + 0.2 != 0.3` — двоичная дробь не представляется точно. Это не баг компилятора.
- Деньги и счётчики — в целых числах (копейки, а не рубли).

### 1.4 `char` — особый тип

- `char`, `signed char`, `unsigned char` — **три разных типа**.
- Знаковость обычного `char` ⚙️ зависит от платформы: x86 — знаковый, ARM часто беззнаковый.
  Проверить: `std::numeric_limits<char>::is_signed`.
- `'A'` — символьная константа: в **C++ имеет тип `char`** (1 байт), в **C — тип `int`** (4 байта).
- `"A"` — строковый литерал: массив из 2 байт (`'A'`, `'\0'`).

### 1.5 Ловушка `long` (LP64 против LLP64)

| Модель | Платформа | `int` | `long` | `long long` | указатель |
|---|---|---|---|---|---|
| **LP64** | Linux, macOS (64-бит) | 4 | **8** | 8 | 8 |
| **LLP64** | Windows x64 (MSVC, MinGW) | 4 | **4** | 8 | 8 |

Практический вывод: **`long` — непереносимый тип**. Хочешь 64 бита — бери `int64_t`.
Хочешь «машинное слово» — `size_t` / `ptrdiff_t` / `intptr_t`.

### 1.6 Как получить границы в коде

```c
/* C */
#include <limits.h>     /* INT_MAX, LLONG_MIN, CHAR_BIT ... */
#include <stdint.h>     /* INT32_MAX, UINT8_MAX ...         */
#include <float.h>      /* FLT_MAX, DBL_EPSILON ...         */
```
```cpp
// C++
#include <limits>
std::numeric_limits<int>::max();        // 2147483647
std::numeric_limits<int>::min();        // -2147483648
std::numeric_limits<unsigned>::max();   // 4294967295
std::numeric_limits<int>::digits;       // 31 — двоичных разрядов мантиссы
std::numeric_limits<double>::epsilon(); // машинный эпсилон
```

### 1.7 Приведения типов

| Синтаксис | Где | Когда применять |
|---|---|---|
| `(int)x` | C-style | не использовать в C++: делает что угодно, включая `const_cast` |
| `static_cast<int>(x)` | C++ | основное: арифметика, явные преобразования |
| `const_cast<T>(x)` | C++ | снять `const` (почти всегда ошибка проектирования) |
| `reinterpret_cast<T>(x)` | C++ | переинтерпретация битов, указатели → целые |
| `dynamic_cast<T>(x)` | C++ | безопасное нисходящее приведение в иерархии классов |

Явное приведение гасит предупреждения `-Wconversion` / `-Wsign-conversion` — но только
если ты **действительно** понял, почему оно нужно.

---

## 2. Ввод-вывод: printf / scanf

### 2.1 Спецификаторы (проверено gcc 12)

| Спец. | Тип | Пример вывода для 255 |
|---|---|---|
| `%d`, `%i` | `int` | `255` |
| `%u` | `unsigned int` | `255` |
| `%x` / `%X` | `unsigned` в hex | `ff` / `FF` |
| `%#x` | hex с префиксом | `0xff` |
| `%o` | восьмеричное | `377` |
| `%ld`, `%lu`, `%lx` | `long` | — |
| `%lld`, `%llu` | `long long` | — |
| `%zu` | **`size_t`** — результат `sizeof`, размеры контейнеров | `8` |
| `%zd` | `ptrdiff_t` | — |
| `%c` | символ (передаётся как `int`) | `A` |
| `%s` | `char *` — печатает до `'\0'` | `hi` |
| `%p` | указатель | `0x7ffd5c1a2b30` (для NULL glibc печатает `(nil)`) |
| `%f` | `double`, 6 знаков | `3.500000` |
| `%e` | экспоненциальная | `3.141590e+00` |
| `%g` | короткая из `%f`/`%e` | `3.14159` |
| `%%` | знак процента | `%` |

⚠️ **Несовпадение спецификатора и типа аргумента — UB**, а не «напечатается ерунда».
Флаг `-Wformat=2` ловит такие ошибки на этапе компиляции.

⚠️ `float` в variadic-функции автоматически приводится к `double`, поэтому `%f` работает
и для `float`. Отдельного `%lf` для печати не нужно (а для `scanf` — нужно, см. 2.3).

### 2.2 Ширина, точность, флаги

Формат: `%[флаги][ширина][.точность][модификатор]спецификатор`

| Пример | Вывод | Что делает |
|---|---|---|
| `%5d` от 42 | `···42` | ширина 5, выравнивание вправо (`·` = пробел) |
| `%-5d` от 42 | `42···` | выравнивание влево |
| `%05d` от 42 | `00042` | заполнение нулями |
| `%+d` от 42 | `+42` | всегда показывать знак |
| `%.2f` от 3.14159 | `3.14` | 2 знака после точки |
| `%8.3f` | `···3.142` | ширина 8, точность 3 |
| `%10s` | строка в поле шириной 10 | |
| `%.*f` | точность берётся из аргумента | |

### 2.3 scanf — главные отличия

```c
int i; double d; char word[32];
scanf("%d %lf %31s", &i, &d, word);   /* прочитает: 42 3.5 hello */
```

- **`&` обязателен** для всех аргументов, кроме массивов (`%s`): массив и так распадается в указатель.
- `double` читается через **`%lf`** (в отличие от печати, где `%f`).
- **Всегда ограничивай ширину строки:** `%31s` для буфера `char[32]`. Без ограничения — переполнение буфера, классическая дыра безопасности.
- `scanf` возвращает **число успешно прочитанных полей** — всегда проверяй: `if (scanf("%d", &x) != 1) { /* ошибка */ }`.
- Оставшийся в буфере `\n` ломает следующий `scanf("%c")`. Лечится пробелом перед спецификатором: `scanf(" %c", &ch)`.

### 2.4 Печать типов фиксированной ширины

```c
#include <inttypes.h>
int32_t a = -42; uint64_t b = 42;
printf("%" PRId32 "\n", a);   // -42
printf("%" PRIu64 "\n", b);   // 42
printf("%" PRIX32 "\n", a);   // FFFFFFD6
```
Без этих макросов код непереносим: `int32_t` на одной платформе может быть `int`, на другой `long`.

### 2.5 C++ потоки

```cpp
#include <iostream>
#include <iomanip>
std::cout << std::setw(5) << std::setfill('0') << 42 << "\n";   // 00042
std::cout << std::fixed << std::setprecision(2) << 3.14159;    // 3.14
std::cout << std::hex << 255 << "\n";                            // ff
int n; std::cin >> n;                                            // проверка: if (std::cin)
```
`"\n"` вместо `std::endl` — `endl` дополнительно сбрасывает буфер и тормозит на больших объёмах.

---

## 3. Массивы, указатели, строки

### 3.1 Главное тождество

```c
a[i]  ≡  *(a + i)  ≡  *(i + a)  ≡  i[a]
```
`3[a]` скомпилируется и даст то же, что `a[3]`. Это не шутка языка, а прямое следствие определения.

### 3.2 Распад массива (array-to-pointer decay)

Имя массива в выражении превращается в указатель на первый элемент. **Исключения**, где
распада НЕ происходит:

| Контекст | Что получаем |
|---|---|
| `sizeof a` | размер всего массива в байтах |
| `&a` | указатель на массив целиком (`int (*)[5]`) |
| строковый литерал как инициализатор `char s[] = "hi"` | массив |

Проверено:
```c
int a[4] = {1,2,3,4};
int *p = a;
sizeof a   // 16   (4 элемента × 4 байта)
sizeof p   // 8    (размер указателя)
```

**Следствие — главная ловушка:** в функцию массив передаётся как указатель, и внутри
функции `sizeof` от параметра всегда даст размер указателя:
```c
void f(int arr[100]) { sizeof arr; }   // 8, а не 400! arr — это int*
```
Поэтому в C длину массива **всегда** передают отдельным параметром. В C++ — `std::size(a)`
или шаблонный параметр.

### 3.3 `char[]` против `char*`

```c
char  s[] = "hello";   // массив 6 байт В СТЕКЕ, можно менять: s[0] = 'H' — ОК
char *p   = "hello";   // указатель на литерал в read-only сегменте
p[0] = 'H';            // UB → segfault (проверено: SIGSEGV, exit 139)
```

| | `char s[] = "hello"` | `char *p = "hello"` |
|---|---|---|
| что создано | массив из 6 `char` | указатель (8 байт) |
| `sizeof` | 6 | 8 |
| можно менять содержимое | ✅ | ❌ UB |
| где живёт | стек | литерал в `.rodata` |

Правильно в C++: `const char *p = "hello";` — тогда компилятор не даст записать.

### 3.4 Арифметика указателей

| Операция | Смысл |
|---|---|
| `p + n` | сдвиг на **n элементов** (адрес + n × sizeof(T)) |
| `p - q` | число элементов между указателями → тип `ptrdiff_t` |
| `p - q` для разных массивов | UB |
| `p++` / `--p` | на один элемент |
| `p == q`, `p < q` | сравнение допустимо **только внутри одного массива** |
| указатель «за концом» (`&a[n]`) | вычислять **можно**, разыменовывать **нельзя** |

### 3.5 Многомерные массивы

```c
int m[3][4];          // 3 строки по 4 int, в памяти ОДИН непрерывный блок из 12 int
m[i][j]  ≡  *(*(m + i) + j)  ≡  ((int*)m)[i * 4 + j]
sizeof m              // 48
sizeof m[0]           // 16 (одна строка)
sizeof m[0][0]        // 4
```
Формула индексации плоского блока: `i * COLS + j`. Именно так правильно делать
динамические матрицы одним `malloc` (быстрее и кэш-дружелюбнее, чем массив указателей).

### 3.6 `const` — читать справа налево

| Объявление | Смысл |
|---|---|
| `const int *p` | указатель на константу: `*p` менять нельзя, `p` можно |
| `int * const p` | константный указатель: `p` менять нельзя, `*p` можно |
| `const int * const p` | нельзя менять ни то, ни другое |
| `int const *p` | то же, что `const int *p` |

Правило хорошего тона: **всё, что функция не меняет, объявляй `const`**.
`double average(const Student *s, size_t n)` — сразу видно, что данные не портятся.

### 3.7 Три способа передать значение в функцию

| Объявление | Вызов | Внутри функции | Меняет оригинал |
|---|---|---|---|
| `void f(int x)` | `f(a)` | копия значения | нет |
| `void f(int *x)` (C, C++) | `f(&a)` | указатель, доступ через `*x` | **да** |
| `void f(int &x)` (только C++) | `f(a)` | ссылка — второе имя `a` | **да** |

Ссылка в C++ не может быть `NULL` и не может быть переназначена — это «указатель без
синтаксиса разыменования». Для out-параметров в чистом C — только указатели.

---

## 4. Переполнение и неопределённое поведение

### 4.1 Signed против unsigned — главное правило курса

> **Беззнаковая арифметика определена как вычисления по модулю 2ᴺ.
> Знаковая арифметика при переполнении — неопределённое поведение.**

| Случай | Поведение | Пример |
|---|---|---|
| `unsigned` переполнение | ✅ определено, wraparound | `3u - 5u` → `4294967294` |
| `signed` переполнение | ❌ UB | `INT_MAX + 1` → gcc даёт −2147483648, но полагаться нельзя |
| приведение signed → unsigned | ✅ определено: по модулю 2ᴺ | `(unsigned)(-1)` → `4294967295` |
| приведение unsigned → signed, значение не влезает | ⚠️ implementation-defined (C++20: модульно) | `(int32_t)(1u<<31)` → −2147483648 |
| унарный минус к unsigned | ✅ определено: `2ᴺ − u` | `-(unsigned)LLONG_MIN` безопасно |

Практические следствия:
- Опасные операции на границах типов делай в `unsigned` (так решается `LLONG_MIN` без спецслучая).
- **Никогда не вычитай из `size_t`/`unsigned` без проверки:**
  ```c
  for (size_t i = 0; i < v.size() - 1; ++i)   /* если size()==0 → 18446744073709551615 итераций */
  for (size_t i = 5; i >= 0; --i)             /* бесконечный цикл; gcc -Wall предупреждает */
  ```
  Правильно: `for (size_t i = 0; i + 1 < n; ++i)` и `for (size_t i = 5; i-- > 0;)`.

### 4.2 Точки следования (sequence points)

UB возникает **внутри одного выражения**, если одна переменная модифицируется дважды
или читается и модифицируется без упорядочивания:

```c
int r = i++ + i++;      // UB: две модификации i в одном выражении
printf("%d %d", i++, i); // UB: порядок вычисления аргументов не определён
```

Но **разные выражения, разделённые `;`, строго упорядочены** — конец полного выражения
является точкой следования:

```c
sum += n % 10;   // выражение 1
n /= 10;         // выражение 2 — гарантированно ПОСЛЕ первого. Законно.
```

Точки следования: `;`, `&&`, `||`, `?:`, запятая (оператор, не разделитель аргументов),
вход и выход из функции.

### 4.3 Деление и остаток (C99+ / C++11+)

| Выражение | Результат | Правило |
|---|---|---|
| `7 / 2` | `3` | целочисленное деление отбрасывает дробь |
| `-7 / 2` | `−3` | округление **к нулю** (не вниз!) |
| `7 / -2` | `−3` | |
| `7 % 2` | `1` | |
| `-7 % 2` | `−1` | знак остатка = знак **делимого** |
| `7 % -2` | `1` | |
| `7 / 2 * 2` | `6` | одинаковый приоритет → слева направо |
| `x / 0`, `x % 0` | UB | |

Гарантировано стандартом: `(a / b) * b + a % b == a`.

**Следствие:** проверка кратности через `n % k == 0` работает и для отрицательных
(`-15 % 3 == 0`), но «остаток» от отрицательного числа отрицателен — для индексов
массива нужен `(n % k + k) % k`.

### 4.4 Каталог классических UB

| # | Действие | Чем ловится |
|---|---|---|
| 1 | Чтение неинициализированной локальной переменной | valgrind, MSan |
| 2 | Выход за границы массива (`a[5]` при `int a[5]`) | ASan: `stack-buffer-overflow` |
| 3 | Переполнение знакового типа | UBSan: `signed integer overflow` |
| 4 | Деление на ноль | UBSan: `division by zero` |
| 5 | Разыменование `NULL`/`nullptr` | ASan: `SEGV` |
| 6 | Use-after-free | ASan: `heap-use-after-free` |
| 7 | Двойное освобождение | ASan: `attempting double-free` |
| 8 | Утечка памяти (не UB, но баг) | ASan LeakSanitizer, valgrind |
| 9 | `i++ + i++`, `f(i++, i)` | gcc: `-Wsequence-point` |
| 10 | Сдвиг на величину ≥ ширины типа или отрицательную | UBSan: `shift exponent` |
| 11 | Запись в строковый литерал | segfault |
| 12 | Несовпадение спецификатора `printf` и аргумента | gcc: `-Wformat=2` |
| 13 | Возврат указателя на локальную переменную | gcc: `-Wreturn-local-addr` |
| 14 | Нарушение strict aliasing (доступ к объекту через указатель чужого типа) | `-fstrict-aliasing` + `-O2`; лечится `memcpy` |
| 15 | Переполнение стека (бесконечная рекурсия, огромный массив) | segfault |

### 4.5 Почему UB опаснее, чем кажется

Компилятор **исходит из того, что UB не происходит**, и строит на этом оптимизации:

```c
if (x + 100 > x) { ... }   // компилятор вправе заменить на if (true):
                           // «если бы было переполнение — это UB, а UB не бывает»
```

Поэтому «у меня работает» в режиме `-O0` ничего не значит. Проверяй сборку с `-O2`
и санитайзерами.

---

## 5. Битовые операции

### 5.1 Операторы

| Оператор | Название | Пример (x = 12 = `0b1100`) | Результат |
|---|---|---|---|
| `&` | И (AND) | `x & 0b1010` | `0b1000` = 8 |
| `\|` | ИЛИ (OR) | `x \| 0b0010` | `0b1110` = 14 |
| `^` | исключающее ИЛИ (XOR) | `x ^ 0b0110` | `0b1010` = 10 |
| `~` | НЕ (инверсия) | `~x` (для 32 бит) | `0xFFFFFFF3` |
| `<<` | сдвиг влево | `1u << 3` | 8 |
| `>>` | сдвиг вправо | `x >> 2` | 3 |

⚠️ **Не путай** `&` (битовое И) и `&&` (логическое И), `|` и `||`, `^` (XOR) и степень.

### 5.2 Шпаргалка по битам (всё проверено на x = 12)

| Задача | Выражение | Пример |
|---|---|---|
| установить бит n | `x \|= (1u << n)` | `12 \| 1` → 13 |
| сбросить бит n | `x &= ~(1u << n)` | `12 & ~4` → 8 |
| инвертировать бит n | `x ^= (1u << n)` | `12 ^ 2` → 14 |
| проверить бит n | `(x >> n) & 1u` или `x & (1u << n)` | бит 2 у 12 → 1 |
| сбросить младший единичный бит | `x & (x - 1)` | `12 & 11` → 8 |
| выделить младший единичный бит | `x & (~x + 1)` или `x & (0u - x)` | `12 & -12` → 4 |
| это степень двойки? | `x && !(x & (x - 1))` | 12→нет, 16→да, 0→нет |
| маска нижних k бит | `(1u << k) - 1` | k=3 → `0x7` |
| округлить вверх до кратного 2ᵏ | `(x + m - 1) & ~(m - 1)` | 13→16, 16→16, 1→8 (m=8) |
| посчитать единицы (popcount) | цикл `while (x) { x &= x-1; ++c; }` | 12→2, 0xFF→8 |

⚠️ **Всегда пиши `1u`, а не `1`** в сдвигах: `1 << 31` для 32-битного `int` — UB
(знаковое переполнение), а `1u << 31` = 2147483648 — корректно. Для 64-бит — `UINT64_C(1) << 40`.

### 5.3 Правила и грабли сдвигов

- Сдвиг на величину **≥ ширины типа** или **отрицательную** — UB. Поэтому в функциях
  работы с битом всегда защита: `if (bit > 31) return flags;`
- `>>` для `unsigned` — логический (заполняется нулями) ✅.
- `>>` для `signed` отрицательных — **арифметический** (заполняется единицами), но до C++20
  это implementation-defined. Не используй сдвиг вправо для деления отрицательных.
- `x >> 1` ≠ `x / 2` для отрицательных: `-7 >> 1` = −4 (округление вниз), `-7 / 2` = −3 (к нулю).

### 5.4 Два дополнения (two's complement)

Отрицательное число = инверсия всех битов + 1:

```
 5        = 0000 0101
~5        = 1111 1010
~5 + 1    = 1111 1011 = -5
```

Следствия:
- Диапазон асимметричен: **отрицательных на одно больше** (−128…127 для 8 бит).
  Поэтому `-(-128)` не представим → UB. Это и есть ловушка `LLONG_MIN`.
- Сложение/вычитание работает одинаково для знаковых и беззнаковых на уровне битов —
  поэтому `(unsigned)(a) + (unsigned)(b)` даёт верные младшие биты суммы.
- `x & (x - 1)` сбрасывает младшую единицу, потому что вычитание 1 превращает
  `…1000` в `…0111`.

### 5.5 Порядок байтов (endianness)

| Порядок | Как лежит `0x12345678` | Где встречается |
|---|---|---|
| **little-endian** | `78 56 34 12` (младший байт по младшему адресу) | x86, x86-64, ARM в обычном режиме |
| **big-endian** | `12 34 56 78` | сетевые протоколы, некоторые MIPS/SPARC, PowerPC |

Проверено на x86-64: **little-endian**.

Определить порядок в коде (единственный полностью легальный способ — через `memcpy`):
```c
#include <string.h>
int is_little_endian(void) {
    const uint16_t probe = 1;
    unsigned char raw[2];
    memcpy(raw, &probe, sizeof raw);
    return raw[0] == 1;
}
```
Через `*(char*)&probe` — нарушение strict aliasing. Через `union` — разрешено в C99+
и на практике работает в C++, но переносимость ниже, чем у `memcpy`.

Смена порядка байт:
```c
uint16_t swap16(uint16_t v) { return (uint16_t)((v << 8) | (v >> 8)); }
uint32_t swap32(uint32_t v) {
    return ((v & 0x000000FFu) << 24) | ((v & 0x0000FF00u) << 8)
         | ((v & 0x00FF0000u) >> 8)  | ((v & 0xFF000000u) >> 24);
}
```
Готовые функции: `htonl`, `htons`, `ntohl`, `ntohs` (`<arpa/inet.h>` в POSIX,
в Windows — `winsock2.h`). «network byte order» = big-endian.

**Почему это важно для тебя:** при чтении бинарных файлов и сетевых пакетов порядок
байтов может не совпадать с машинным. И при записи `struct` в файл целиком ты пишешь
и дыры выравнивания (см. 6.1) — так делать нельзя.

---

## 6. Структуры и память

### 6.1 Выравнивание (alignment) — проверено

```c
struct demo { char a; int b; char c; };
sizeof(struct demo)      // 12, а не 6!
```

Раскладка: `a` (1 байт) → **3 байта дыры** → `b` (4 байта) → `c` (1 байт) → **3 байта
хвостового заполнения**, чтобы массив таких структур сохранял выравнивание.

| Структура | `sizeof` | Комментарий |
|---|---|---|
| `{char; int; char}` | 12 | 6 байт данных + 6 заполнения |
| `{char; char}` | 2 | заполнения нет |
| `{char; double}` | 16 | double требует выравнивания 8 |
| `{char; int; char}` `__attribute__((packed))` | 6 | без заполнения, но доступ медленнее и непереносимо |

Правило: **упорядочивай поля от больших к меньшим** — структура станет компактнее
(проверено: 24 байта против 16):
```c
struct bad  { char a; int b; char c; double d; };  // sizeof = 24
struct good { double d; int b; char a; char c; };  // sizeof = 16
```

Следствия:
- **Никогда не пиши структуру в файл/сеть побайтово целиком** — запишешь и дыры
  (мусор, неинициализированная память, непереносимо). Пиши поле за полем или используй
  явную упаковку битов (как в задаче M2-T2).
- `memcmp` двух структур может отличаться из-за разного мусора в дырах.
- `sizeof` структуры ≥ суммы размеров полей.

### 6.2 Стек против кучи

| | Стек | Куча |
|---|---|---|
| кто выделяет | компилятор автоматически | ты (`malloc`/`new`) |
| скорость | очень быстро (сдвиг указателя) | медленно (поиск блока) |
| время жизни | до выхода из блока `{}` | пока не освободишь |
| размер | ограничен (обычно 1–8 МБ) | ограничен памятью |
| ошибки | переполнение стека | утечки, use-after-free, фрагментация |
| возврат указателя из функции | ❌ UB | ✅ можно |

```c
int *bad(void)  { int a[100]; return a; }        // UB: a умрёт при выходе
int *good(void) { int *p = malloc(100 * sizeof *p); return p; }  // ОК, вызывающий освобождает
```

### 6.3 `malloc`/`free` против `new`/`delete`

| | C: `malloc` | C++: `new` |
|---|---|---|
| выделение | `int *p = malloc(n * sizeof *p);` | `int *p = new int[n];` |
| возвращает | `void*` (в C приводится неявно) | `int*` |
| вызывает конструкторы | ❌ нет | ✅ да |
| инициализация нулём | `calloc(n, size)` | `new int[n]()` со скобками |
| освобождение | `free(p)` | `delete[] p` (для массива!), `delete p` (для одного) |
| при неудаче | возвращает `NULL` | бросает `std::bad_alloc` |

Правила, которые нельзя нарушать:
- `malloc` ↔ `free`, `new` ↔ `delete`, `new[]` ↔ `delete[]`. Смешивать — UB.
- `free(NULL)` и `delete nullptr` — безопасно (ничего не делают).
- Двойное освобождение — UB. После `free(p)` ставь `p = NULL`.
- `sizeof *p` вместо `sizeof(int)` — не ошибёшься при смене типа.

### 6.4 RAII и правило 0/3/5 (C++)

**RAII**: ресурс захватывается в конструкторе и освобождается в деструкторе. Деструктор
вызывается автоматически при выходе из области видимости → утечка невозможна.

- **Правило 0**: если класс не владеет ресурсом — не пиши ни деструктор, ни копирующие операции.
- **Правило 3**: если написал одно из {деструктор, copy-конструктор, copy-присваивание} —
  пиши все три.
- **Правило 5**: + move-конструктор и move-присваивание (C++11).

```cpp
class Bad {                       // классическая ошибка
    int *data;
public:
    Bad(size_t n) : data(new int[n]) {}
    ~Bad() { delete[] data; }     // а copy-конструктор не определён!
};
Bad a(10);
Bad b = a;                        // поверхностное копирование: b.data == a.data
                                  // при выходе из области — двойной delete[] → UB
```
Лечение: либо правило 3/5, либо «правило 0» + `std::vector`/`std::unique_ptr` вместо голого указателя.

---

## 7. Компиляция и инструменты

### 7.1 Флаги — что ловит каждый

| Флаг | Что даёт |
|---|---|
| `-std=c11` / `-std=c++20` | стандарт языка. Без него — «gnu17» с расширениями |
| `-Wall` | базовый набор предупреждений (включая `-Wmisleading-indentation`, `-Wtype-limits`, `-Wreturn-local-addr`) |
| `-Wextra` | ещё больше: неиспользуемые параметры, знаки сравнения |
| `-Wpedantic` | ругается на нестандартные расширения |
| `-Wshadow` | локальная переменная затеняет другую с тем же именем |
| `-Wconversion` | неявные сужающие преобразования |
| `-Wsign-conversion` | неявная смена знаковости |
| `-Wformat=2` | проверка `printf`/`scanf` на соответствие аргументов |
| `-Wnull-dereference` | разыменование возможного NULL |
| `-g` | отладочная информация (нужна для gdb и читаемых сообщений санитайзеров) |
| `-O0` / `-O2` / `-O3` | оптимизация. При учёбе — `-O0`, UB проявляется иначе |
| `-fsanitize=address` | ASan: границы памяти, use-after-free, утечки |
| `-fsanitize=undefined` | UBSan: переполнения, сдвиги, деление на 0, NULL |
| `-fno-omit-frame-pointer` | читаемые стектрейсы в санитайзерах |
| `-MMD -MP` | генерация зависимостей для `make` |

**Рабочая сборка на время учёбы:**
```bash
g++ -std=c++20 -Wall -Wextra -Wpedantic -Wshadow -Wconversion -Wsign-conversion \
    -Wformat=2 -g -fsanitize=address,undefined -fno-omit-frame-pointer main.cpp -o app
gcc -std=c11   ...те же флаги... main.c -o app -lm
```

Цель — **ноль предупреждений всегда**. Предупреждение, которое ты «привык игнорировать»,
маскирует то, которое однажды окажется настоящим багом.

### 7.2 Санитайзеры против valgrind

| Инструмент | Скорость | Что ловит |
|---|---|---|
| ASan (`-fsanitize=address`) | ~2× медленнее | выход за границы, use-after-free, double-free, **утечки** |
| UBSan (`-fsanitize=undefined`) | почти бесплатно | знаковое переполнение, сдвиги, деление на 0, выравнивание |
| TSan (`-fsanitize=thread`) | 5–15× | гонки данных в многопоточном коде |
| MSan | 3× | чтение неинициализированной памяти (нужен clang) |
| valgrind (memcheck) | 20–50× | то же, что ASan+MSan, но **без перекомпиляции** |

ASan/UBSan/TSan **нельзя включать одновременно** (ASan+UBSan — можно).
Полезные переменные окружения:
```bash
ASAN_OPTIONS=detect_leaks=1:halt_on_error=0
UBSAN_OPTIONS=print_stacktrace=1
```

### 7.3 gdb — необходимый минимум

```bash
gcc -g prog.c -o prog      # -g обязателен
gdb ./prog
```

| Команда | Коротко | Действие |
|---|---|---|
| `break main` | `b main` | точка останова (или `b file.c:23`, `b func`) |
| `run` | `r` | запустить (аргументы: `run arg1 arg2`) |
| `next` | `n` | шаг, **не заходя** в функции |
| `step` | `s` | шаг, **заходя** в функции |
| `continue` | `c` | до следующей точки останова |
| `finish` | | до конца текущей функции |
| `print x` | `p x` | значение (`p *arr@10` — 10 элементов массива) |
| `info locals` | `i locals` | все локальные переменные |
| `backtrace` | `bt` | стек вызовов |
| `frame 2` | `f 2` | перейти в кадр №2 |
| `watch x` | | останов при изменении `x` |
| `list` | `l` | исходник вокруг текущей строки |
| `quit` | `q` | выход |

Полезный режим: `gdb --args ./prog arg1`, а при падении — `gdb ./prog core`.

### 7.4 git — необходимый минимум

```bash
git status                      # что изменилось
git add file.c                  # в индекс (или git add -A — всё)
git commit -m "сообщение"       # зафиксировать
git push                        # отправить на сервер
git pull                        # забрать изменения
git log --oneline -10           # история кратко
git diff                        # непри commit'ленные изменения
git diff HEAD~1                 # сравнить с предыдущим коммитом
git restore file.c              # отменить изменения в рабочем каталоге
git rm --cached file            # убрать из индекса, оставив на диске
git clean -nd                   # показать неудалённые лишние файлы (-fd — удалить)
```

Коммит — одна логическая единица. Сообщения в повелительном наклонении:
«добавить is_prime», а не «фикс» и не «изменения».

**В репозиторий не кладут:** скомпилированные бинарники, объектные файлы,
`build/`, `.vscode/` (кроме `tasks.json`/`launch.json`), `.idea/`, `.DS_Store`.
Это всё — в `.gitignore`.

### 7.5 make — минимальный Makefile

```makefile
CC      := gcc
CFLAGS  := -std=c11 -Wall -Wextra -Wpedantic -Wshadow -g -fsanitize=address,undefined
LDLIBS  := -lm
SRC     := $(wildcard *.c)
OBJ     := $(SRC:.c=.o)
TARGET  := app

all: $(TARGET)

$(TARGET): $(OBJ)
	$(CC) $(CFLAGS) $^ -o $@ $(LDLIBS)

%.o: %.c
	$(CC) $(CFLAGS) -MMD -MP -c $< -o $@

-include $(OBJ:.o=.d)

run: $(TARGET)
	./$(TARGET)

clean:
	rm -f $(OBJ) $(OBJ:.o=.d) $(TARGET)

.PHONY: all run clean
```

⚠️ Отступы в рецептах — **обязательно табуляция**, не пробелы.
`$@` — цель, `$^` — все зависимости, `$<` — первая зависимость.

---

## 8. Отличия C от C++

| Тема | C | C++ |
|---|---|---|
| Тип `'A'` | `int` (4 байта) | `char` (1 байт) |
| `sizeof('A')` | **4** | **1** |
| `void*` → `T*` | неявно | нужен `static_cast` |
| `NULL` | `((void*)0)` | `0`; **используй `nullptr`** |
| Объявление `f()` | функция с неопределённым числом аргументов | функция без аргументов |
| `struct Foo` в объявлении | обязательно писать `struct Foo` | можно просто `Foo` |
| `const int N = 5; int a[N];` | VLA, допустимо | constexpr-контекст, иначе ошибка |
| Компилятор проверяет объявление функции | нет (C89) / да (C99) | да, всегда |
| Строковый литерал → `char*` | разрешено (плохая практика) | запрещено (нужен `const char*`) |
| Проверка типов variadic | слабее | строже |

Практический вывод: **C++ не является надмножеством C**. Почти любой корректный C-код
компилируется как C++, но не наоборот (тип `'A'`, `void*`, `const` в объявлении массива).

---

## 9. Каталог частых ошибок

| Симптом | Причина | Лечение |
|---|---|---|
| `segmentation fault` | разыменование NULL/мусорного указателя, выход за границы, запись в литерал | ASan покажет точную строку |
| Функция возвращает мусор | вернул указатель на локальную переменную | выделяй в куче или передавай буфер |
| `sizeof` даёт 8 вместо размера массива | массив распался в указатель | передавай размер отдельным параметром |
| Бесконечный цикл `for (size_t i = n; i >= 0; --i)` | `size_t` беззнаковый, `>= 0` всегда true | `for (size_t i = n; i-- > 0;)` |
| `v.size() - 1` даёт огромное число | вычитание из `size_t` при пустом контейнере | сначала `if (!v.empty())` |
| Строка печатается мусором после `char s[10]` | нет `'\0'` | `s[n] = '\0'` или `snprintf` |
| `scanf("%c")` не ждёт ввода | в буфере остался `\n` | `scanf(" %c", &ch)` — пробел перед `%c` |
| `if (x = 5)` всегда истинно | присваивание вместо сравнения | gcc с `-Wall` предупреждает (`-Wparentheses`); запись `if (5 == x)` превращает опечатку в ошибку компиляции |
| Утечка памяти | забыл `free`/`delete`, перезаписал указатель | LeakSanitizer; в C++ — `unique_ptr` |
| `delete` вместо `delete[]` для массива | UB | массив → всегда `delete[]` |
| Сравнение float через `==` | округление | `fabs(a-b) < 1e-9` |
| `-Wsign-compare` в `for (int i = 0; i < v.size(); ++i)` | `int` против `size_t` | `size_t i = 0` |
| Код работает в Debug, падает в Release | UB, которое «проявилось» при оптимизации | UBSan + `-O2` на этапе разработки |

---

## 10. Словарь терминов

| Термин | Значение |
|---|---|
| **UB** (undefined behavior) | поведение не определено стандартом: компилятор вправе сделать что угодно, включая удаление кода |
| **Implementation-defined** | поведение зависит от реализации, но задокументировано (например, знаковость `char`) |
| **Decay / распад массива** | неявное превращение имени массива в указатель на первый элемент |
| **Sequence point / точка следования** | момент, до которого все побочные эффекты завершены |
| **Two's complement / два дополнения** | способ представления отрицательных чисел: инверсия + 1 |
| **Endianness / порядок байтов** | порядок следования байтов числа в памяти |
| **Strict aliasing** | правило: обращаться к объекту можно только через указатель совместимого типа (исключения — `char*` и `memcpy`) |
| **Alignment / выравнивание** | требование, чтобы адрес объекта был кратен его размеру; источник «дыр» в структурах |
| **Padding** | байты-заполнители между полями структуры |
| **RAII** | Resource Acquisition Is Initialization: ресурс живёт столько, сколько объект |
| **Правило 0/3/5** | сколько специальных функций класса нужно определить |
| **Dangling pointer** | указатель на освобождённую/несуществующую память |
| **Memory leak** | выделенная память, указатель на которую потерян |
| **Sentinel** | специальное значение-ограничитель (например, `'\0'` в строках) |
| **LSB / MSB** | least/most significant bit — младший/старший бит |
| **popcount** | число единичных битов |
| **VLA** | variable-length array — массив переменной длины (C99, в C++ нет) |
| **POD / trivially copyable** | тип, который можно безопасно копировать побайтово |

---

## 11. Журнал изменений

### v1.0 — 2026-09-14
Первая версия. Собрано из разбора диагностики и ревью L0-T1:
- § 1 Типы данных: размеры и пределы (проверено gcc 12.2), `stdint.h`, `float`/`double`,
  ловушка `long` (LP64/LLP64), `char` как особый тип, приведения.
- § 2 `printf`/`scanf`: все спецификаторы с реальными выводами, ширина/точность,
  `%lf` в scanf, `inttypes.h`, C++-потоки.
- § 3 Массивы и указатели: `a[i] ≡ *(a+i)`, decay и его исключения, `sizeof` массива
  против указателя, `char[]` против `char*`, арифметика указателей, многомерные массивы,
  `const`, три способа передачи в функцию.
- § 4 Переполнение и UB: signed против unsigned, точки следования, таблица деления и
  остатка для отрицательных, каталог из 15 классических UB, почему UB ломает оптимизации.
- § 5 Битовые операции: операторы, шпаргалка трюков (всё с проверенными числами для x = 12),
  правила сдвигов, two's complement, endianness с легальным способом определения.
- § 6 Структуры и память: выравнивание на реальных примерах, стек против кучи,
  `malloc` против `new`, RAII и правило 0/3/5.
- § 7 Инструменты: таблица флагов компиляции, санитайзеры против valgrind, gdb-минимум,
  git-минимум, готовый Makefile.
- § 8 Отличия C от C++ · § 9 Каталог частых ошибок · § 10 Словарь терминов.

**Появится в следующих версиях:** строки C++ (`std::string`, `string_view`), контейнеры
и итераторы STL, алгоритмы и лямбды, шаблоны, исключения, умные указатели, move-семантика,
многопоточность, классы и наследование, таблицы сложности алгоритмов.
